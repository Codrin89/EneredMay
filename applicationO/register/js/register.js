document.getElementById("register").addEventListener('click', function () {

    //validate fields
    if (document.getElementById('password').value.length.trim() === 0) {
        //set error to email
    }


})
